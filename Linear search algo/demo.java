
import java.util.Arrays;

public class demo {
    public static void main(String[] args) {
        int[] arr = {1,2,3,4,5};

        int index = Arrays.binarySearch(arr, 2); // Returns the index of 42 or a negative value if not found

        System.out.println(index);
        
       //boolean areEqual = Arrays.equals(arr1, arr2); // Compares both arrays for equality

        

    }
}
